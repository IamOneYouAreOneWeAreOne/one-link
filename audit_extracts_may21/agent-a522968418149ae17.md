"We don't peek" and "We can't help you there" are doctrine violations (adversarial / framing-by-what-we-can't-do, per the `feedback_positive_forward_framing.md` memory rule).

OK I have a comprehensive picture now. Let me deliver the final findings.

---

**One Link Web UI Audit — `src/one_link/web/index.html` (30,934 lines)**

1. **HIGH — XSS via `e.message` in folder-conflicts modal** — `index.html:16757-16758`. `$("#conflicts-list").innerHTML = ` `` `<div class="empty">Failed to load conflicts: ${e.message || e}</div>` ``. `e.message` originates from server JSON (`j.error`/`j.message` at `_apiError`, line 10086) and is injected raw. A compromised/malformed `/api/folder-conflicts` response with `{"error": "<img src=x onerror=alert(1)>"}` executes. **Fix:** swap to `el("div", "empty", "Failed to load conflicts: " + errorToastBody(e))` and `list.replaceChildren(div)`.

2. **HIGH — Same XSS pattern in 5 sibling error renderers** — `11757` (discover modal), `18994` (recovery host), `25076` (storage totals), `28935` (sovereignty body), `15646` (file preview). All do `innerHTML = ...${e.message || e}...`. Same trust assumption that server-supplied error strings are HTML-safe; they aren't. **Fix:** route every error string through `_escapeHtml()` or build with `el()`.

3. **HIGH — Inline `onerror` handler in QR `<img>` injects raw HTML** — `index.html:12022-12024`. `qrHost.innerHTML = `…`onerror="this.parentNode.innerHTML='<div…>QR generation failed</div>'"`…`. Survives only because no CSP `script-src` blocks inline handlers, but the moment the project lands a CSP this breaks silently and users see a broken QR with no error UI. Also blocks tightening CSP. **Fix:** attach `img.addEventListener("error", () => {qrHost.replaceChildren(el("div", "qr-fail", "QR generation failed"))})` programmatically.

4. **HIGH — Session token written to `location.href` on session-expired reload** — `index.html:10224`: `location.href = location.pathname + "?t=" + encodeURIComponent(token);`. The full token lands in browser history, in the referrer of any subsequent navigation, in any browser-sync (Chrome Sync, Firefox Sync), and in any extension that reads URL. Same token persists across daemon restarts per the comment at 10112. **Fix:** POST the token to `/api/session/refresh` to set the cookie, then `location.reload()` — never put it in a GET URL.

5. **MEDIUM — `document.querySelector` with un-escaped peer-controlled `m.id`** — `index.html:22811`. Unlike sibling paths (14912, 19374, 23493) which wrap with `CSS.escape`, this one doesn't. A message id containing a quote or backslash (legacy / malformed payloads) throws `SyntaxError` and the entire surrounding async handler aborts mid-flight, leaving UI in a half-rendered state. **Fix:** `CSS.escape(m.id)` like the others.

6. **MEDIUM — Race in `state.messages` mutation across WS handlers** — `index.html:20174-20349`. `msg_edit`, `msg_delete`, `reaction`, and `msg` all do `state.messages.find(x => x.id === ...)` then mutate in place, but `scheduleRenderMessages` is rAF-coalesced. If `msg_delete` fires after an in-flight `_markBubbleState` for the same id, `body = null` survives but `_sendingState = "sent"` was applied — the bubble renders as "[message deleted]" with a ✓ tick. Also at 20402 the slice `state.messages.slice(-800)` can drop a bubble that an in-flight WS edit was about to patch, then the find returns undefined silently. **Fix:** index `state.messages` by id (Map) and version-stamp mutations; or queue WS events behind the rAF the same way renders are.

7. **MEDIUM — `transferForMessage` is O(N·M) per render** — `index.html:12716-12725`. Walks `state.transfersById.values()` (up to 80) inside `renderFileBubble`, called once per message bubble (up to 200 visible). 16k iterations per re-render, fires 10+ times/sec during an active transfer (line 20464). Build a `Map<dirBlob, transfer>` at WS-event time. **Fix:** maintain `state.transferByDirBlob` keyed at the `transfer` handler.

8. **MEDIUM — Misleading "Drag the original in again" toast at `openMessageFile`** — `index.html:12404-12409`. With the new `/api/outbound_files/<transfer_id>` endpoint, this branch only fires when **both** the browser cache is gone AND no `transfer_id` exists — i.e., legacy pre-ledger bubbles only. But the toast says "Drag the original in again to share another copy", which is *also* misleading: the user might not have the original anymore, and "share another copy" implies an action that creates a new transfer for the same content. Same wording-lies-about-cause pattern as the one already fixed. **Fix:** "This file came from an older version of One Link, before the transfer ledger existed. There's no link back to disk for it." (no action verb).

9. **MEDIUM — Em-dashes in user-facing copy violate `feedback_no_em_dashes.md`** — User-facing strings (not comments): line 9991 ("Channel needs renegotiation — sends will queue…"), 11158 ("…let it run for a few seconds — the daemons re-handshake automatically."), 12405 ("Can't preview this file — it predates…"), 19394 ("Send button never silently hangs — every…"), 23980 ("Couldn't copy — clipboard access denied."), 11156 (interpolated lead text). **Fix:** replace with periods or commas; for "Send button never silently hangs" rewrite to positive forward framing.

10. **MEDIUM — Doctrine violation (adversarial / "what-we-can't" framing)** — `index.html:7620` ("Files from paired devices land here. We don't peek."), `9105` ("We can't help you there."), `8565` ("…We won't conjure it."). All three frame the work by what we're not / can't do — per `feedback_positive_forward_framing.md` and `feedback_no_personal_identifiers_in_public_code.md`'s positive-framing arm. **Fix:** "Files from paired devices land here." (drop the second sentence — silence is the strongest signal). "Each member keeps their own history on their own device." "Pick a folder that already exists and is writable."

11. **MEDIUM — `state.transfers.slice(0, 80)` discards in-flight WS state** — `index.html:20454`. After every transfer event the array is hard-capped at 80 but `state.transfersById` Map (line 20455) is **not** pruned in lockstep, growing without bound across a long session. After a soak, the Map holds thousands of stale entries; `transferForMessage` walks all of them. **Fix:** prune `transfersById` to match the array after the slice, or use only the Map and derive the array.

12. **MEDIUM — `URL.createObjectURL` for cached outbound files never revoked on tab close** — `index.html:12159` allocates a blob URL per outbound image preview; `rememberOutboundFile` at 12145 only revokes when the *same key* is re-added. A session that sends 200 photos and never re-sends the same name accumulates 200 retained Blob references (and the underlying Files) for the lifetime of the tab. **Fix:** evict + revoke from `outboundFileCache` on FILE_DONE WS event (the daemon now persists the path; the browser-side blob is redundant once the ledger has it).

13. **LOW — `setInterval` cluster at 19586-19591 never cleared on auth loss** — six polling timers (5/4/8/15/30/12 s) keep firing after a 401, generating a storm of `_maybe401` calls that all try `_attemptAutoRecovery` simultaneously. The `_autoRecoveryInFlight` guard helps but doesn't prevent the post-recovery flood. **Fix:** pause all six on the first 401 and resume on successful recovery / banner dismissal.

14. **LOW — Accessibility: no focus trap in modals built via `innerHTML`** — modal panes at 11240 (version-mismatch), 11392 (camera/mic permission), 19468 (what's new), 20920 (image editor), 21438 (slash commands), 22417 (recovery confirm), 23211 (voice preview) set raw `innerHTML` with backdrop+pane structure but rely on `data-*-close` click handlers. Tab leaves the modal into the underlying page (which is `aria-hidden=false`), and Escape works on some but not all. **Fix:** common `openModal(node)` that captures focus and restores on close; mark page `inert` while shown.

15. **LOW — Empty state corruption: `renderFileBubble` on a transfer with `total_bytes=0`** — `index.html:15484`. `Math.max(0, (t.total_bytes || 0) - (t.progress_bytes || 0))` is fine but `t.progress_pct` (15478) becomes NaN if computed upstream as `done/total` with total=0, the CSS `width: NaN%` resolves to 0 silently. The transfer-facts collapsed pill still says "0 B / 0 B · ETA …". A genuinely zero-byte file (a `touch` to send) ends up in "Sending…" forever because `status` never moves to `complete` if the daemon also has a divide-by-zero edge. **Fix:** short-circuit `total === 0 → label = "Empty file, sending…"` and ensure the daemon emits `complete` for zero-byte transfers.

**Files referenced:** `c:\Users\Josh\Projects\Coherence\One_link\src\one_link\web\index.html`