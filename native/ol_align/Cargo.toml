[package]
name = "ol_align"
version = "0.22.1"
edition = "2021"
description = "One Link: Gaussian alignment trust A(x,t) = exp(-(hop^2 + staleness^2)/L_session) per Equation of ONE"
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "ol_align"
crate-type = ["lib"]

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
