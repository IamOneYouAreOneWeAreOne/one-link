[package]
name = "ol_canon"
version = "0.21.0-alpha.0"
edition = "2021"
description = "One Link file engine: canonical-bytes encoder (Phase 0 substrate, ported from std.codec.canon)."
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "ol_canon"
crate-type = ["lib"]

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest  = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "canon_bench"
harness = false

[lints]
workspace = true
