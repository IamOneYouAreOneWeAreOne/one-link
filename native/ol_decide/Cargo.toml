[package]
name = "ol_decide"
version = "0.22.0-alpha.0"
edition = "2021"
description = "One Link: universal Decide trait + Context struct for per-event context-aware decisions"
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "ol_decide"
crate-type = ["lib"]

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true
