[package]
name = "ol_routing"
version = "0.22.1"
edition = "2021"
description = "One Link file engine: tau_c-weighted routing primitives (Phase D, ADR-0028). Harvested from OneField mesh/routing.cl."
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "ol_routing"
crate-type = ["lib"]

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest  = { workspace = true }
criterion = { workspace = true }

[[bench]]
name    = "routing_bench"
harness = false

[lints]
workspace = true
