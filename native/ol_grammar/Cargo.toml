[package]
name = "ol_grammar"
version = "0.22.1"
edition = "2021"
description = "One Link file engine: Re-Pair grammar compression for secondary chunk index (Phase D, ADR-0030)."
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "ol_grammar"
crate-type = ["lib"]

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest  = { workspace = true }
criterion = { workspace = true }

[[bench]]
name    = "grammar_bench"
harness = false

[lints]
workspace = true
