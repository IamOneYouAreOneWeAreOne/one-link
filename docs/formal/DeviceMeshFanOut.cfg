SPECIFICATION Spec
\* With an active attacker, every source may fail. That terminal trace is an
\* expected availability outcome; the configured properties are safety only.
CHECK_DEADLOCK FALSE

CONSTANTS
    Sources      = {S1, S2, S3}
    Chunks       = {C1, C2, C3, C4}
    K_THRESHOLD  = 2
    AttackerOn   = TRUE

SYMMETRY Symmetry

INVARIANTS
    TypeOK
    NoDoubleAssignSameSource
    ProgressMonotone
    FailedSourceCantDeliver
